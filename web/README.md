# OpenBrain Web

Next.js UI for OpenBrain.

```bash
npm install
npm run dev
```

Open http://localhost:3777. The app expects the API at `http://localhost:8000` by default; override with `NEXT_PUBLIC_API_BASE`.
